import { Metadata } from "next";

export const metadata: Metadata = {
  title: "ABVS | Election Results",
}

export default function ElectionResults() {
  return (
    <div>Election Results here</div>
  );
}